@model IEnumerable<Projekt_Dejtingsida.Models.FriendRequestModels>

@{
    ViewBag.Title = "OutgoingRequests";
}

<h2>Outgoing requests</h2>
<table class="table">
    <tr>
        <th>
            To:
        </th>
        <th>
        </th>

    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Person2)
        </td>
        <td>
            @Html.ActionLink("Remove", "", "", null, new { @class = "btn btn-primary" })
        </td>
    </tr>
}

</table>
