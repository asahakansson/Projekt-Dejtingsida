@model Projekt_Dejtingsida.Models.RequestSent
@{
    ViewBag.Title = "RequestList";
}

@if (Model.Success)
{
    <div class="alert alert-success" role="alert">
        Friend request added.
    </div>
}
else
{
    <div class="alert alert-danger" role="alert">
        @Model.Error
    </div>
}